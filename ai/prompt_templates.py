"""
ai/prompt_templates.py
--------------------------
STUB — Phase 4. Will hold the system prompt(s) used by ai_analyzer.py,
kept separate from the calling code so prompts can be versioned/tuned
independently. See the master system prompt design in the project's
planning docs for the rules the AI layer must follow (explanation only,
no price prediction, no fabricated data).
"""
